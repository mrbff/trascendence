/// <reference types="@swagger-api/apidom-core/types/minim" />
import { Element } from '@swagger-api/apidom-core';
export declare class AncestorLineage<T extends Element> extends Array<WeakSet<T>> {
    includesCycle(element: T): boolean;
    includes(searchElement: WeakSet<T> | T, fromIndex?: number): boolean;
}
